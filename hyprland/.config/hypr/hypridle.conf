# Timeouts in seconds
idle_timeout = 300         # 5 min
dpms_timeout = 600         # 10 min

# What to run on idle
on_idle = hyprctl dispatch dpms off; ~/.config/hypr/scripts/lock.sh

# What to run on wake
on_wakeup = hyprctl dispatch dpms on
